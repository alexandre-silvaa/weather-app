import DashboardPresentation from "./presentation";

export default function DashboardContainer(): React.JSX.Element {
  const title = "Dashboard";
  return <DashboardPresentation title={title} />;
}
